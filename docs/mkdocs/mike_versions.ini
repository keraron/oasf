# Mike version labels (edit by hand).
#
# local   — task docs:mike:deploy-local (no git push)
# release — task docs:mike:deploy / CI; bump when publishing a new docs release.

[versions]
local = dev
release = v1.0.0
